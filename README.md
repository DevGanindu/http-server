# project02 (httpServer)

Build:

```bash
make
```

Run:

```bash
./server
# or
make run
```

Test (from another shell):

```bash
curl http://localhost:8080
```

The server serves [index.html](index.html) from the project root.
# http-server
# http-server
